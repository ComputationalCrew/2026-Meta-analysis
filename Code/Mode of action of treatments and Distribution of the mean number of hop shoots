# Load necessary library
library(dplyr)
library(ggplot2)
library(lme4)
library(emmeans)
library(tidyr)
library(readr)
library(pivottabler)
library(dplyr)
library(stats)
library(NMA)
library(multinma)
library(dplyr)
library(metafor)
library(writexl)

# Upload the raw data

downy <- read.csv(".../Downy Mildew Fungicide Meta Analysis Database_Github.csv", check.names=FALSE)

# Data manipulation
downy  <- downy  %>%
  mutate(MOA = ifelse(MOA %in% c("BM01", "BM02"), "BM", MOA))

# Table 1. Mode of action of treatments 
# Get count of number of plots that evaluated a given MOA in each state
dm_count <- downy %>%
  group_by(MOA, State) %>%
  summarise(count = n())

qpvt(downy, c("MOA", "State"), "Trial", "n()")

# Figure 1 Distribution of the mean number of hop shoots 

ggplot(downy, aes(x = Spikes, fill = Treat))+
  geom_density(alpha = 0.5) +
  labs(x = "Spikes per plant", y = "Density") +
  theme_classic() + labs(fill = "MOA") +
  scale_x_continuous(limits = c(0, 200))
dev.off()

ggplot(downy, aes(x = tResponse_ratio, fill = Treat)) +
  geom_density(alpha = 0.5) +
  labs(x = "Response ratio", y = "Density") +
  theme_classic() + labs(fill = "MOA") +
  scale_x_continuous(limits = c(-6, 1.5))
dev.off()

# If Oregon,
downy_OR= subset(downy, State == "Oregon")

# If Washington,
downy_WA= subset(downy, State == "Washington")
